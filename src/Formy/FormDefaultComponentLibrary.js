import {
   Checkbox,
   Radio,
   RadioGroup,
   Select,
   Textarea,
   TextField,
} from 'toolbox';

const FormFieldComponentLibrary = {
   text: TextField,
   email: TextField,
   password: TextField,
   number: TextField,
   checkbox: Checkbox,
   radio: Radio,
   textarea: Textarea,
   radiogroup: RadioGroup,
   select: Select,
};

export default FormFieldComponentLibrary;
